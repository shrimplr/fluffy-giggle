function love.conf(t)
	t.identity = nil
	t.version = "11.3"
	t.console = false                   -- Attach a console (boolean, Windows only)
	t.window.title = "Untitled"
	t.window.width = 800
	t.window.height = 600
end